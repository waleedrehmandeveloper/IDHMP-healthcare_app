import 'package:flutter/material.dart';

class AppColors {

  // Main background
  static Color background = Color(0xFFF6FAFC);

  // Main buttons / primary actions
  static Color primary = Color(0xFF3076F0);

  // Bagdes / BagdeColor actions
  static Color badgebg = Color(0xFFdde4f0);

  // Secondary buttons / highlights
  static Color secondary = Color(0xFF68FCA8);

  // Main text
  static Color text = Color(0xFF102A43);

  // Cards / containers
  static Color white = Color(0xFFFFFFFF);
  // Text / btns / icons on primary background
  static Color onPrimary = Color(0xFFFFFFFF);

  // Secondary text
  static Color textSecondary = Color(0xFF627D98);

  // Borders
  static Color border = Color(0xFFD9E7EF);
}