import 'package:flutter/material.dart';

class OpdTokenView extends StatelessWidget {
  const OpdTokenView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}